import React, { useState } from 'react'
import { Link } from 'react-router-dom'
export default function Navbar({openMenu}) {

    return (
        <div className={openMenu ? 'sidenav active' : 'active'}>
             
        </div>
    )
}
